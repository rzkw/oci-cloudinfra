# oci-cloudinfra

Configuration, documentation etc of my virtual network on OCI.

## Modules

Auto-generated docs for each module are in `terraform/<module>/README.md`.

- **VCN** — virtual cloud network, subnets, routing, security lists
- **Instances** — compute (A1.Flex)
- **Budget** — cost alerts (email)
